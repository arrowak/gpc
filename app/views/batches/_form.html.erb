<%= form_for [@institute, @department, @batch] do |f| %>
  <% if @batch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@batch.errors.count, "error") %> prohibited this batch from being saved:</h2>

      <ul>
      <% @batch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date_start %><br />
    <%= f.text_field :date_start %>
  </div>
  <div class="field">
    <%= f.label :date_end %><br />
    <%= f.text_field :date_end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
