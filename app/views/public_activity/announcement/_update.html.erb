<div class="listactivities">
<% if activity.trackable %>
	
	<%= link_to activity.owner.email, user_path(activity.owner), :class => "userprofilelink" if activity.owner %>
	has updated
	<%= link_to "announcement", batch_announcement_path(activity.trackable.batch, activity.trackable) %>

<% else %>
  which has since been removed
<% end %>
</div>